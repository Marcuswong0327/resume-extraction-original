{
  "name": "resume-extraction",
  "version": "1.0.0",
  "private": true,
  "description": "Streamlit resume parser. Python dependencies live in requirements.txt (used by Streamlit Cloud).",
  "scripts": {
    "start": "python -m streamlit run app.py",
    "start:open": "python -m streamlit run app.py --browser.gatherUsageStats false"
  }
}
