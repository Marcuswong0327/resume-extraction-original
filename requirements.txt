# Python deps for Streamlit Community Cloud / pip-based deploy (see package.json for convenience scripts).
# Legacy .doc: antiword is NOT a pip package — do not add it here. Install via repo packages.txt
# (line: antiword) for Streamlit Cloud / Linux apt; see package.json description.
# Optional later: aspose-words-foss (MIT) when PyPI has a wheel for your Python version.
streamlit>=1.28.0
requests>=2.28.0
PyPDF2>=3.0.0
python-docx>=1.0.0
pandas>=2.0.0
openpyxl>=3.1.0
python-dateutil>=2.8.0
numpy>=1.24.0
